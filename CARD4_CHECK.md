# 카드 4 — 실제 결함 세 개

검사 대상: 자기소개 공개 화면

## 수정한 결함

### 1. 밝은 배경의 작은 강조 문구 명암 부족

- 수정 전: `ABOUT ME`의 명암비가 **4.26:1**로 일반 텍스트 기준 4.5:1에 미달했다.
- 수정 후: 밝은 배경용 강조색을 조정해 **5.73:1**로 높였다.

### 2. 어두운 배경의 작은 강조 문구 명암 부족

- 수정 전: `THREE THINGS ABOUT ME`의 명암비가 **3.76:1**로 일반 텍스트 기준 4.5:1에 미달했다.
- 수정 후: 어두운 배경 전용 강조색을 적용해 **6.78:1**로 높였다.

### 3. 전체 메뉴와 본문 영역의 구조 미분리

- 수정 전: 전체 메뉴가 `main` 안에 포함되어 있었고, 키보드 사용자가 본문으로 바로 이동할 링크가 없었다.
- 수정 후: 전체 메뉴를 `main` 밖으로 분리하고, 첫 번째 Tab 위치에 `본문으로 바로가기` 링크를 추가했다.

## 재검사 결과

- 링크: `본문으로 바로가기`, `JW.`, `소개`, `취향과 강점`의 연결 대상이 모두 존재한다.
- 키보드: 첫 번째 Tab에서 `본문으로 바로가기`가 선택되고, 이후 전체 메뉴 링크로 이동한다.
- 제목 구조: `H1 → H2 → H3` 순서이며 단계 건너뜀이 없다.
- 명암: 작은 강조 문구를 포함한 검사 대상이 모두 기준을 통과했다.
- 가로 넘침: 1366px급 화면에서 없음.
- 브라우저 콘솔: 사이트에서 발생한 오류 **0건**. 브라우저 확장 프로그램 자체 로그는 사이트 오류에서 제외했다.

